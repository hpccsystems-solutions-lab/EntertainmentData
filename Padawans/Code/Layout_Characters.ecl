EXPORT Layout_Characters := RECORD
    STRING name;
    STRING height;
    STRING mass;
    STRING hair_color;
    STRING skin_color;
    STRING eye_color;
    STRING birth_year;
    STRING sex;
    STRING gender;
    STRING homeworld;
    STRING species;
    STRING films;
    STRING vehicles;
    STRING starships;
END;